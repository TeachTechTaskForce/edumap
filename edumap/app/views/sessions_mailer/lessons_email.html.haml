!!!
%html
  %head
    %meta{ charset: "utf-8", "content-type" => "text/html", "http-equiv" => "Content-Type"}
  %body
    %h1 Your EDUMAP Saved Lessons
    %table
      %thead
        %tr
          %th Curriculum
          %th Lesson
          %th Standards
          %th Level
      %tbody
        - @lessons.each do |lesson|
          %tr
            %td= lesson.curriculum.name
            %td= link_to lesson.name, lesson.lesson_url
            %td= lesson.codes.pluck(:identifier).join(", ")
            %td= lesson.level_list
